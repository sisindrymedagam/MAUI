<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:icon="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             xmlns:views="clr-namespace:Passkeeper.Features.MFACodes.Views"
             xmlns:components="clr-namespace:Passkeeper.Components"
             xmlns:helpers="clr-namespace:Passkeeper.Helpers"
             x:Class="Passkeeper.Features.MFACodes.Pages.MFACodesListPage"
             Title="2FA Codes"
             BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray950}}">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <helpers:IsCollectionEmptyConverter x:Key="IsCollectionEmptyConverter"/>
            <helpers:FirstLetterConverter x:Key="FirstLetterConverter"/>
            <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <helpers:BoolToColorConverter x:Key="BoolToColorConverter"/>
            
            <!-- Modern Card Style -->
            <Style x:Key="ModernCardStyle" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=White, Dark={StaticResource Gray900}}" />
                <Setter Property="Stroke" Value="Transparent" />
                <Setter Property="StrokeThickness" Value="0" />
                <Setter Property="StrokeShape" Value="RoundRectangle 16" />
                <Setter Property="Margin" Value="16,8" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Shadow">
                    <Setter.Value>
                        <Shadow Brush="{AppThemeBinding Light=#20000000, Dark=#40FFFFFF}" 
                                Offset="0,2" 
                                Radius="8" 
                                Opacity="0.1" />
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Modern Label Styles -->
            <Style x:Key="TitleLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Gray900}, Dark=White}" />
                <Setter Property="LineBreakMode" Value="TailTruncation" />
            </Style>
            
            <Style x:Key="SubtitleLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}" />
                <Setter Property="LineBreakMode" Value="TailTruncation" />
            </Style>
            
            <Style x:Key="CaptionLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}" />
                <Setter Property="LineBreakMode" Value="TailTruncation" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <Grid>
        <!-- Empty State -->
        <VerticalStackLayout IsVisible="{Binding MFACodes.Count, Converter={StaticResource IsCollectionEmptyConverter}}"
                            HorizontalOptions="Center" 
                            VerticalOptions="Center" 
                            Spacing="24"
                            Padding="32">
            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray800}}"
                    StrokeShape="RoundRectangle 50"
                    WidthRequest="100"
                    HeightRequest="100"
                    HorizontalOptions="Center">
                <Border.Stroke>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}" Offset="0" />
                        <GradientStop Color="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}" Offset="1" />
                    </LinearGradientBrush>
                </Border.Stroke>
                <Label Text="🔐"
                       FontSize="40"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </Border>
            
            <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                <Label Text="No authenticator codes yet"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark=White}"
                       HorizontalOptions="Center" />
                <Label Text="Tap the + button to add your first authenticator code"
                       FontSize="16"
                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </VerticalStackLayout>
        
        <!-- MFA Codes List -->
        <RefreshView IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}"
                     BackgroundColor="Transparent">
            <CollectionView ItemsSource="{Binding MFACodes}"
                           SelectionMode="None"
                           BackgroundColor="Transparent">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems Mode="Reveal">
                                    <SwipeItem Text="Edit" 
                                              BackgroundColor="{AppThemeBinding Light={StaticResource Blue500}, Dark={StaticResource Blue400}}"
                                              CommandParameter="{Binding .}" 
                                              Invoked="OnEditMFACode"/>
                                    <SwipeItem Text="Delete" 
                                              BackgroundColor="{AppThemeBinding Light={StaticResource Red500}, Dark={StaticResource Red400}}"
                                              CommandParameter="{Binding .}" 
                                              Invoked="OnDeleteMFACode"/>
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <views:MFACodeItemView />
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <!-- Floating Action Button -->
        <Button Grid.Row="2"
        icon:MauiIcon.Value="{icon:Fluent Icon=Add24, IconSize=30, IconColor=White}"
        TextColor="White"
        FontSize="32"
        BackgroundColor="#512BD4"
        WidthRequest="64"
        HeightRequest="64"
        CornerRadius="50"
        VerticalOptions="End"
        HorizontalOptions="End"
        Margin="24" 
        Clicked="OnAddCodeClicked"
        ToolTipProperties.Text="Add new authenticator code">
            <Button.Shadow>
                <Shadow Brush="#40000000"
                Offset="0,5"
                Radius="10"
                Opacity="0.5" />
            </Button.Shadow>
        </Button>

        <!-- Add Code Bottom Sheet -->
        <components:BottomSheetComponent x:Name="AddCodePanel" 
                                        OnShow="HideTabBar" 
                                        OnHidden="ShowTabBar">
            <components:BottomSheetComponent.SheetContent>
                <VerticalStackLayout Padding="20" Spacing="20">
                    
                    <Label Text="Add Authenticator Code" 
                           FontSize="24" 
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                    
                    <Label Text="Enter the details for your authenticator code" 
                           FontSize="14"
                           TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}" />
                    
                    <Entry x:Name="NameEntry" 
                           Placeholder="Account Name (e.g., Google, GitHub)"
                           PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                    
                    <Entry x:Name="IssuerEntry" 
                           Placeholder="Issuer (optional)"
                           PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                    
                    <Entry x:Name="SecretEntry" 
                           Placeholder="Secret Key"
                           PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                           TextColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                    
                    <HorizontalStackLayout Spacing="12">
                        <Button Text="Cancel" 
                                Style="{StaticResource SecondaryButton}"
                                HorizontalOptions="FillAndExpand"
                                Clicked="OnCancelAdd" />
                        <Button Text="Add Code" 
                                Style="{StaticResource PrimaryButton}"
                                HorizontalOptions="FillAndExpand"
                                Clicked="OnConfirmAdd" />
                    </HorizontalStackLayout>
                    
                </VerticalStackLayout>
            </components:BottomSheetComponent.SheetContent>
        </components:BottomSheetComponent>
        
    </Grid>
    
</ContentPage>